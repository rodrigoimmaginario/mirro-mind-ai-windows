---
digest: >
  v0.22.1 is a validation patch for v0.22.0. It preserves the Mode Orientation
  and Test Hygiene behavior and applies the formatting correction required by
  the repository-wide CI format check.
---

# v0.22.1 — Mode Orientation CI Formatting

**Date:** 2026-06-06

## Highlights

- Preserves the v0.22.0 mode orientation and `/mm-discard` behavior.
- Reformats the touched conversation-logger test file so CI format checks pass.
- Keeps v0.22.0 as the feature release and v0.22.1 as the validation patch.

## Where We Started

After promoting v0.22.0, GitHub Actions failed at the repository-wide `ruff
format --check src tests` step. Local focused tests and lint had passed, but the
full format gate surfaced one unformatted test file.

## What Changed

This patch applies the formatting correction and bumps the package version to
v0.22.1 so the stable channel can move forward through a clean release artifact.

## Conscious Exclusions

- No behavior changes beyond the v0.22.0 release content.
- No additional Explorer Mode scope.

## What We Learned

Run the repository-wide format check before promotion, not only focused lint and
tests.

## Next Horizon

Continue CV16 with the Explorer activation contract and in-session exploratory
state now that the mode foundation is packaged.
